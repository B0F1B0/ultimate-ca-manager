export default {
  helpContent: {
    title: "系统日志",
    subtitle: "服务器自身的应用程序日志",
    overview: "无需登录主机即可回读 UCM 自己写下的内容。这是解释那些从未生成记录的协议故障的日志：在校验阶段被拒绝的 SCEP 注册，会在请求行存在之前就被拒绝，因此只出现在这里。",
    sections: [
      {
        title: "来源",
        items: [
          "应用程序 — UCM 自身的日志。唯一带有组件的来源",
          "访问 — Gunicorn 访问日志：HTTP 请求与状态码，仅限原生安装",
          "错误 — Gunicorn 错误日志：worker 启动与未处理的回溯，仅限原生安装",
          "Journal — systemd 单元日志，前提是服务用户有权读取",
        ]
      },
      {
        title: "筛选",
        items: [
          "来源 — 读取哪个日志。只提供本部署实际具备的来源",
          "组件 — 应用程序日志的某个子系统，或全部。列表包含全部子系统，而不只是屏幕上出现的",
          "日志级别 — 是下限而非精确匹配：WARNING 同时显示错误与严重",
          "搜索 — 不区分大小写，匹配消息和组件名",
          "排除 — 移除命中的行：让重复心跳安静下来的最快方式",
          "正则表达式 — 把搜索和排除按模式处理；写了一半的模式不匹配任何内容",
          "日期 — 起止时间窗口，在服务端应用",
          "行数 — 返回多少条匹配行，最新在前",
        ]
      },
      {
        title: "实时日志",
        items: [
          "每五秒轮询一次；最新的一行排在最前",
          "清除日期窗口，因为它提出的是相反的问题",
        ]
      },
    ],
    tips: [
      "时间戳采用服务器本地时间且不含偏移；时区在页面底部标明",
      "不符合已知格式的行仍会显示（无级别），而不是被隐藏",
      "一个回溯是一条记录，而不是每行一条：消息单元格会换行完整显示",
      "密钥等敏感内容在离开进程前已在服务端脱敏",
    ],
    warnings: [
      "近期没有输出的组件仍会列出：选中它可能什么都不返回",
      "读取仅限管理员，且刻意不做审计：审计记录会写入同一个日志",
    ],
  },
  helpGuides: {
    title: "系统日志",
    content: `
## 概述

无需登录主机即可回读 UCM 自己写下的内容。这是解释那些从未生成记录的故障的日志：在校验阶段被拒绝的 SCEP 注册，会在请求行存在之前就被拒绝。

行按最新在前显示，所有筛选都在服务端应用。

## 来源

**来源** 和 **组件** 位于筛选面板中：先选日志，再缩小到某个子系统。

- **应用程序**：UCM 自身的日志。唯一带有组件的来源。
- **访问**：Gunicorn 访问日志，仅限原生安装。
- **错误**：Gunicorn 错误日志，仅限原生安装。
- **Journal**：systemd 日志，在可读取时提供。

### 组件

**所有组件** 即整个应用程序日志。列表包含 UCM 可以写入的所有子系统，即使近期没有输出也能选择。选中某项即涵盖其下全部：\`services\` 同时匹配 \`services.scep.scep_service\`。

## 筛选

### 日志级别
是下限而非精确匹配：**WARNING** 同时显示错误与严重。无法识别级别的行永远不会被隐藏。

### 搜索与排除
两者都匹配消息和组件名。**排除** 会移除命中的行，这是让每分钟重复的心跳安静下来的最快方式。开启 **正则表达式** 后两者都按模式处理；写了一半的模式不会匹配任何内容，而不是让请求失败。

### 日期
起止时间窗口。设置边界时，没有时间戳的行会被排除：窗口询问的是某个时刻。

### 行数
返回多少条匹配行，从 100 到 5000。

## 实时日志

每五秒轮询一次，并清除日期窗口，因为它提出的是相反的问题。

## 复制

**复制全部** 取走屏幕上的所有行；勾选任意行后变为 **复制所选**。两者都按日志原本的形式复制。

## 读懂页脚

- **正在显示 M 条匹配行中最新的 N 条** — 匹配的行数超过了 **行数** 设置。
- **仅读取了文件最新的部分** — 文件比读取窗口更大。

时间戳本身不含偏移，它们是服务器本地时间，时区标注在路径旁边。

## 访问

仅限管理员，且刻意不做审计：审计记录会写入同一个日志。
`
  }
}
